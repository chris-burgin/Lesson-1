// declare a variable that can change and one that cannot
let canChange = "Meme"
const cantChange = "weebs"

// modify the change variable
canChange = "NotMeme"

// print both variables to the console
console.log(canChange)
console.log(cantChange)

// print the variables in the same console log
console.log(canChange + cantChange)
